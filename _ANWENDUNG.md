# Anwendung der Korrekturen

Основа комплекта: commit `34ff53d`.

1. Распаковать архив в корень локального репозитория `AP1_VB`.
2. Разрешить замену существующих файлов.
3. Удалить дублирующий файл `QUELLEN.md` из корня репозитория.
4. Оставить канонический файл `00-organisation/QUELLEN.md`.
5. Проверить изменения командой `git status` и затем создать commit.

Рекомендуемое название commit:

```text
Korrigiere Struktur Metadaten und Lernstand
```

Комплект не создаёт папки `02-ap2-fiae` и `03-projekt-und-muendliche-pruefung`.
